Постановка задачи

Написать программу с консольным интерфейсом, которая выполняет слежение за выбранными файлами.

Ограничимся  двумя характеристиками за изменениями которых выполняется слежение  :

1. Существование файла;
2. Размер файла.

Программа будет выводить на консоль уведомление о произошедших изменениях в файле.

Существует несколько ситуаций для наблюдаемого файла:

1. Файл существует - на экран выводится факт существования файла и его  размер.
2. Файл существует, файл был изменен - на экран выводится факт существования файла, сообщение о том что файл был изменен и его размер.  
3. Файл не существует - на экран выводится информация о том что файл не существует.

При возникновении изменения состояния наблюдаемого файла ( возникновение события ), необходимо выводить на экран соответствующее сообщение.

В данной реализации используем механизм сигнально-слотового соединения для обеспечения обработки события изменения наблюдаемого файла.

Можно использовать бесконечный цикл, в котором будем обновлять состояние объекта (наблюдаемого файла/файлов) 100 миллисекунд

(например так  std::this_thread::sleep_for( std::chrono::milliseconds( 100 ) );).


Комментарии к задаче:


в начальный момент времени когда передан момент слежения(указан адрес файла)
 
нужно в тот же момент передать состояние файла (должен быть механизм исключения из под наблюдения)


1 ситуация возможна при подключениии наблюдения за файлом и когда файла не было и он создался


При возникновении изменения состояния наблюдаемого файла ( возникновение события ), необходимо выводить на экран соответствующее сообщение. (ЛОГИРОВАНИЕ)

тесты:  создание; 1
	переименование; 3
	переименование обратно и не изменен; 1
	если между первым и вторым был изменен размер; 2

После постановки задачи идет определение с какими данными мы работаем(формат входных данных). в данном случае это пути к файлам-строкой 

-строка может иметь недопустимые символы => ошибка: недопустимая строка, но программа должна продолжить работать с уже заданными файлами

-строка можеть быть именем файла, значит он должен находиться в папке программы

-строка может быть путем, но заканчиваться на папке


сущности : файл, файлменеджер, то с помощью чего логируем

библиотека QFileInfo class
работает с типом QString 


refresh()-обновляет состояние


QTextStream
